def sum_of_series(start, finish):
    result = 0
    n = start
    while n <= finish:
        print('new iteration !!!')
        print(n)
        result += n
        n += 1
        print(result)
    return result
print(sum_of_series(3, 5))
