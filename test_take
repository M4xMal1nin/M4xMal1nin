assert take(['one', 'two', 8], 9) == ['one', 'two', 8]
assert take([1, 2]) == [1]
assert take(['one', 'two', 'three'], 2) == ['one', 'two']
assert take(['one', 'two', 'three'], 0) == []
assert take([]) == []
